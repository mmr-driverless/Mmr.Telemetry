namespace Mmr.Telemetry.Services.Enums
{
    public enum MessageDataTypeEnum
    {
        BYTE, SHORT, UINT8, UINT32_T, LONGLONG, FLOAT, DOUBLE, BOOL, EBS_ENUM, AS_ENUM, MISSION_ENUM, DRIVE_ENUM, NONE
    }
}
